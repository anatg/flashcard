2) display overall score
3) get colors working
4) input emacs commands
5) when a user gets an answer wrong, make sure that you display that one a bit more often
6) allow user to add cards to the deck
7) keep the deck in a database
